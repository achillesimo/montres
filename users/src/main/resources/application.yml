spring:
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
         uri: https://github.com/achillesimo/montres
         searchPaths: services-configuration
  application:
    name: utilisateurs