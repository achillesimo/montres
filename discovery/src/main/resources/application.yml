server:
  port: 8761
eureka:
  client:
    register-with-eureka: false # Desactive l'autorenregistrement
    fetch-registry: false # Desactive l'autorenregistrement
logging:
  level:
    com:
      netflix:
        eureka: OFF # Supprime les log erreur
        discovery: OFF # Supprime les log erreur
